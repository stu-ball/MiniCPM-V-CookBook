# 🛠️ 开发者支持指南（处理用户反馈）

> 当用户反馈摄像头问题时，如何快速定位和解决

---

## 📋 用户反馈处理流程

### 第 1 步：让用户发送 vConsole 截图

告诉用户：
```
请您打开页面上的 vConsole（绿色按钮），
开始视频通话，然后截图发给我，谢谢！
```

用户会看到自动诊断结果，无需输入任何命令。

---

### 第 2 步：识别问题类型

根据用户截图中的诊断结果判断：

#### 情况 1: 显示 `✅✅✅ 诊断通过！`

```
✅✅✅ 诊断通过！✅✅✅
当前使用的是推荐镜头: ✅ Android 主摄
```

**判断：** 摄像头选择正确，问题可能在其他地方

**处理方法：**
1. 询问用户具体问题（画面模糊？卡顿？黑屏？）
2. 检查网络状况（WebRTC 连接质量）
3. 检查设备性能（低端设备可能画质差）
4. 检查光线条件（环境太暗会影响画质）

---

#### 情况 2: 显示 `⚠️⚠️⚠️ 检测到问题！`

```
⚠️⚠️⚠️ 检测到问题！⚠️⚠️⚠️
当前使用了不推荐的镜头: 🔍 ⚠️ 长焦镜头 (Telephoto)

📱 设备信息:
   当前镜头: 🔍 ⚠️ 长焦镜头 (Telephoto)
   设备型号: Android
   浏览器: Mozilla/5.0 (Linux; Android 12; vivo ...)
```

**判断：** 浏览器选择了错误的镜头

**处理方法（2 种）：**

##### 方法 A：远程协助用户切换（推荐）

告诉用户：
```
您好，我看到您的摄像头选择有问题。
请帮我做一个简单操作：

1. 在页面上找到"翻转摄像头"按钮（通常是一个循环箭头图标）
2. 点击它，切换摄像头
3. 再截图给我看一下 vConsole 的诊断结果

如果没有"翻转摄像头"按钮，请刷新页面重新开始通话试试。
```

用户切换后，系统会自动诊断新镜头，如果显示 `✅ 切换成功！` 说明问题解决。

##### 方法 B：发送修复脚本（开发/测试环境）

如果在开发或测试环境，可以发送以下脚本给用户：

```javascript
// Android 设备修复脚本
(async () => {
    console.log('🔧 开始修复摄像头...');
    const result = await forceLensSelection('main');
    if (result.success) {
        console.log('✅ 修复成功！请刷新页面重新测试');
        alert('修复成功！请刷新页面重新测试');
    } else {
        console.error('❌ 修复失败:', result.error);
        alert('修复失败，请联系技术支持');
    }
})();
```

```javascript
// iOS 设备修复脚本
(async () => {
    console.log('🔧 开始修复摄像头...');
    const result = await forceLensSelection('wide');
    if (result.success) {
        console.log('✅ 修复成功！请刷新页面重新测试');
        alert('修复成功！请刷新页面重新测试');
    } else {
        console.error('❌ 修复失败:', result.error);
        alert('修复失败，请联系技术支持');
    }
})();
```

**使用方法：**
1. 让用户打开 vConsole
2. 切换到 "Sources" 或 "Console" 标签
3. 粘贴上述脚本并执行
4. 等待提示刷新页面

---

#### 情况 3: 显示 `⚪ 诊断结果: 未知镜头类型`

```
⚪ 诊断结果: 未知镜头类型
如果视频画面有问题，请将诊断信息截图发给技术支持
```

**判断：** 无法识别镜头类型（可能是新设备或特殊设备）

**处理方法：**
1. 查看完整的诊断信息（设备型号、浏览器版本）
2. 查看"所有可用摄像头"列表，识别镜头类型
3. 提交给开发团队，增强镜头识别规则
4. 临时方案：让用户尝试切换摄像头

---

### 第 3 步：验证问题是否解决

让用户：
1. 刷新页面，重新开始视频通话
2. 截图 vConsole 的诊断结果
3. 如果显示 `✅✅✅ 诊断通过！` 说明问题解决

---

## 🎯 快速诊断命令（开发者专用）

如果您有远程访问权限，可以在浏览器控制台直接运行：

### 1. 查看所有摄像头

```javascript
await listAllCameras()
```

**输出示例（vivo 手机）：**
```
📱 可用摄像头:
┌───┬──────────┬──────────────────┬─────────────────┬───────────┬────────┐
│序号│ 设备ID   │      标签        │      类型       │   警告    │  推荐  │
├───┼──────────┼──────────────────┼─────────────────┼───────────┼────────┤
│ 0 │ xxx...   │ Front Camera     │ 🤳 前置摄像头   │           │ ⚪ 可用│
│ 1 │ yyy...   │ Back Camera (0)  │ ✅ Android 主摄 │           │ ✅ 推荐│
│ 2 │ zzz...   │ Back Camera (2)  │ 🔍 长焦镜头     │ 会导致放大│ ❌ 不推荐│
└───┴──────────┴──────────────────┴─────────────────┴───────────┴────────┘
```

---

### 2. 检查当前镜头

```javascript
await checkCurrentCamera()
```

**输出示例：**
```
📸 [当前摄像头检查]
🎥 当前摄像头:
- 镜头类型: 🔍 ⚠️ 长焦镜头 (Telephoto)
- 分辨率: 960x540

💡 诊断建议:
⚠️ 检测到可能使用长焦镜头，这会导致画面放大！
   解决方案：运行 forceLensSelection("main") 强制切换到主摄。
```

---

### 3. 强制切换镜头

```javascript
// Android 设备（vivo/小米/华为/OPPO）
await forceLensSelection('main')

// iOS 设备（iPhone）
await forceLensSelection('wide')

// 超广角（如果需要更宽视野）
await forceLensSelection('ultrawide')
```

---

### 4. 清除摄像头缓存

```javascript
clearCameraCache()
```

清除后，刷新页面会重新检测摄像头。

---

## 📊 常见问题对照表

| 用户反馈 | 可能原因 | 诊断结果 | 解决方案 |
|---------|---------|---------|----------|
| 画面明显放大，视野变窄 | 使用了长焦镜头 | 🔍 ⚠️ 长焦镜头 | 切换摄像头或运行 `forceLensSelection('main')` |
| 画面很宽，比相机 App 宽 | 使用了超广角 | 📐 超广角 | 可能是正常的，询问用户是否接受 |
| 画面模糊，只能近距离清晰 | 使用了微距镜头 | 🔬 ⚠️ 微距镜头 | 切换摄像头或运行 `forceLensSelection('main')` |
| 画面黑白或很暗 | 使用了景深镜头 | 📷 ⚠️ 景深镜头 | 切换摄像头或运行 `forceLensSelection('main')` |
| 画面卡顿 | 网络问题 | ✅ 诊断通过 | 检查网络连接，降低分辨率 |
| 画面黑屏 | 权限问题或设备问题 | - | 检查浏览器权限，重启浏览器 |

---

## 🔍 诊断信息字段说明

用户截图中会包含以下关键信息：

### 1. 镜头类型

| 图标 | 含义 | 是否正常 |
|-----|------|---------|
| ✅ | 推荐镜头（主摄/广角） | ✅ 正常 |
| 🤳 | 前置摄像头 | ✅ 正常 |
| 🔍 ⚠️ | 长焦镜头 | ❌ 异常 |
| 🔬 ⚠️ | 微距镜头 | ❌ 异常 |
| 📷 ⚠️ | 景深镜头 | ❌ 异常 |
| 📐 | 超广角 | ⚪ 特殊 |
| ❓ | 未知 | ⚪ 需分析 |

---

### 2. 设备型号

用户截图会显示：
```
设备型号: Android 或 iPhone
```

可以通过浏览器 User-Agent 进一步识别具体型号：
- `vivo` - vivo 手机
- `MI` / `Xiaomi` - 小米手机
- `HUAWEI` - 华为手机
- `OPPO` - OPPO 手机
- `iPhone` - iPhone

---

### 3. 浏览器信息

```
浏览器: Mozilla/5.0 (Linux; Android 12; vivo ...)
```

常见浏览器识别：
- `Chrome` - Chrome 浏览器
- `Safari` - Safari 浏览器
- `MiuiBrowser` - 小米浏览器
- `UCBrowser` - UC 浏览器

---

## 🚀 自动修复机制

代码中已实现**开发环境自动修复**：

```javascript
// 在 localhost、127.0.0.1 或包含 test 的域名下
// 检测到异常镜头时，会自动尝试切换到推荐镜头
const isDevMode = window.location.hostname === 'localhost' || 
                  window.location.hostname.includes('127.0.0.1') ||
                  window.location.hostname.includes('test');

if (isDevMode && 异常镜头) {
    console.warn('🔧 检测到开发环境，尝试自动修复...');
    await forceLensSelection(isAndroid ? 'main' : 'wide');
}
```

**生产环境不会自动修复**，避免误操作。

---

## 📝 反馈记录模板

收到用户反馈后，建议记录以下信息：

```
用户反馈时间: 2026-01-18 14:30
用户设备: vivo X90 Pro
浏览器: Chrome 120
诊断结果: 🔍 ⚠️ 长焦镜头 (Telephoto)
用户描述: 画面明显放大，看到的范围比相机 App 小
处理方法: 指导用户切换摄像头
结果: 切换后显示 ✅ Android 主摄，问题解决
备注: 该设备默认选择长焦，需在代码中增加 vivo X90 Pro 的特殊识别规则
```

---

## 🔧 代码优化建议（针对特定设备）

如果某个设备型号频繁出现问题，可以增加特殊识别规则：

### 示例：vivo X90 Pro 总是选择长焦

在 `initVideoDevices()` 函数中添加：

```javascript
// 特殊设备识别
const userAgent = navigator.userAgent.toLowerCase();
const isVivoX90Pro = userAgent.includes('vivo') && userAgent.includes('x90');

if (isVivoX90Pro) {
    console.log('🎯 检测到 vivo X90 Pro，使用特殊策略');
    // 优先选择 deviceId 而不是 facingMode
    // 强制排除 camera 2（长焦）
}
```

---

## 📚 相关文档

- [用户指南](./VCONSOLE_USER_GUIDE.md) - 发给用户的操作指南
- [技术文档](./CAMERA_COMPATIBILITY_GUIDE.md) - 完整的技术实现说明
- [代码文件](./src/hooks/useLiveKit.js) - 核心实现代码

---

## ❓ 常见开发者问题

### Q1: 自动诊断什么时候运行？

**A:** 
- 视频通话开始时（`joinRoom()` 成功后 500ms）
- 切换摄像头后（`switchCamera()` 成功后 500ms）

---

### Q2: 如何禁用自动诊断？

**A:** 
在 `joinRoom()` 函数中，注释掉以下代码块：
```javascript
// 🔥 自动诊断摄像头（视频模式下）
if (mode === 'video' && tracks.find(t => t.kind === 'video')) {
    // ... 诊断代码
}
```

---

### Q3: 如何添加新的镜头识别规则？

**A:** 
在 `initVideoDevices()` 函数中添加识别关键词：
```javascript
// 识别 XXX 品牌的特殊镜头
else if (label.includes('xxx_keyword')) {
    cameraType = 'XXX 特殊镜头';
    priority = 8;
}
```

---

### Q4: 诊断结果能发送到服务器吗？

**A:** 
可以！在自动诊断代码中添加 API 调用：
```javascript
if (cameraType.includes('⚠️')) {
    // 发送异常镜头数据到服务器
    await fetch('/api/report-camera-issue', {
        method: 'POST',
        body: JSON.stringify({
            userId: userId,
            cameraType: cameraType,
            deviceInfo: navigator.userAgent,
            timestamp: Date.now()
        })
    });
}
```

---

## 🎉 总结

✅ **用户无需输入命令**，自动诊断  
✅ **清晰的诊断结果**，一目了然  
✅ **远程协助流程**，快速解决问题  
✅ **开发者工具**，深度调试支持

处理用户反馈只需 3 步：**看截图 → 识别问题 → 指导解决**
