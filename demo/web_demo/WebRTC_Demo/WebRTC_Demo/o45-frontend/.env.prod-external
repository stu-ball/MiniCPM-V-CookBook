# 生产环境 - 对外版本（本地部署）
NODE_ENV=production
VITE_APP_VERSION=external
# LiveKit 通过反向代理（/rtc → ws://localhost:7880/rtc）
# 前端自动根据页面协议使用 wss:// 或 ws://
VITE_LIVEKIT_URL=/rtc
# Current C++ inference mode: "duplex" or "simplex" (set automatically by oneclick.sh)
VITE_CPP_MODE=duplex
