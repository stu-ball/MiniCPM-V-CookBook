# ✅ 立即测试 - 三步搞定

## 问题：`window.__testNoRobotTimeout()` 没反应？

### 解决方案（复制粘贴到控制台）

```javascript
// 方式1：强制触发（推荐，随时可用）
window.__testNoRobotTimeout(true)

// 方式2：查看状态后再决定
window.__debugInfo()
```

---

## 🎯 现在就试试

1. **打开项目页面**（视频或语音通话页面）

2. **按 F12 打开控制台**

3. **复制粘贴这行代码并回车：**
```javascript
window.__testNoRobotTimeout(true)
```

4. **看到弹窗就成功了！** ✅
   ```
   连接超时，未能成功连接到服务，将自动挂断。
   ```

---

## 📝 预期看到的日志

```
🧪 Video组件：手动触发无机器人超时测试 { force: true }
🧪 测试：triggerNoRobotTimeout 被调用 { hasTimer: false, force: true }
🧪 测试：强制模式，即使没有定时器也触发超时
🧪 触发结果: true
🧪 执行挂断流程...
```

然后会弹出提示框，点击确定后自动挂断。

---

## 🔧 如果还是不行

### 检查1：测试函数是否存在
```javascript
typeof window.__testNoRobotTimeout
// 应该输出: 'function'
// 如果是 'undefined'，刷新页面重试
```

### 检查2：查看完整状态
```javascript
window.__debugInfo()
// 会显示表格，包含所有状态信息
```

### 检查3：确认开发模式
```
确保运行的是: npm run dev
不是: npm run build
```

---

## ⚡ 其他可用命令

```javascript
// 查看定时器状态
window.__checkTimerStatus()

// 查看完整调试信息
window.__debugInfo()

// 正常测试（需要先开始通话）
window.__testNoRobotTimeout()
```

---

## 📚 更多文档

- `测试备忘单.md` - 快速参考
- `快速测试指南.md` - 详细说明
- `TEST_NO_ROBOT_TIMEOUT.md` - 完整文档

---

---

## ✅ 环境自动识别

**无需手动配置！** 代码会自动判断：

- 🏠 **localhost 开发环境**：10秒超时（快速测试）
- 🌍 **生产环境**：3分钟超时（正常使用）

上线时无需修改任何代码！

---

**现在就试试吧！** 🚀

